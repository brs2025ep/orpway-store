import { ParcelarEmPipe } from './parcelar-em.pipe';

describe('ParcelarEmPipe', () => {
  it('create an instance', () => {
    const pipe = new ParcelarEmPipe();
    expect(pipe).toBeTruthy();
  });
});
